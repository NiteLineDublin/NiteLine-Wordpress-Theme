# NiteLine Wordpress Theme
Wordpress Theme for NiteLine Dublin

### Setup

Install dependencies: `$ npm install`

### Build

- Production (with minification): `$ gulp build:prod`
- Local development (no minification): `$ gulp build:dev`
